class Mass {
    public:
        double m; // mass
        double x_0; // initial location
        int xIndex; // index of x in the global system matrix
        int xDotIndex; // index of xDot in the global system matrix
};